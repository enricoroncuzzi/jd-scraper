# OpenRouter API key (https://openrouter.ai/settings/keys) - used by src/scorer.py for
# scraper job scoring. Free-tier models only; see src/scorer.py's model constants.
LLM_API_KEY=
GROQ_API_KEY=
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
OUTPUT_PATH=output
DEDUP_LOG_PATH=
DATABASE_URL=

# CV source content tailor.py assembles from - not part of this repo (personal
# content). Required on any machine other than the one the fallback path in
# tailor.py's _DEFAULT_MASTER/_DEFAULT_CSS was hardcoded for. See AGENTS.md.
CV_MASTER_PATH=
CV_CSS_PATH=
JD_OUTPUT_ROOT=
